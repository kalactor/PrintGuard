<Window
    x:Class="PrintGuardWinUI.Windows.PasswordPromptWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Printing blocked">
    <Border Padding="18">
        <StackPanel>
            <TextBlock Text="PrintGuard blocked a print job. Enter password to continue."
                       FontSize="16"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"
                       Margin="0,0,0,10" />

            <TextBlock x:Name="ModeText"
                       Foreground="Gray"
                       TextWrapping="Wrap"
                       Visibility="Collapsed"
                       Margin="0,0,0,10" />

            <TextBlock Text="Printer" FontWeight="SemiBold" Margin="0,4,0,0" />
            <TextBlock x:Name="PrinterText" TextWrapping="Wrap" Margin="0,0,0,8" />

            <TextBlock Text="Document" FontWeight="SemiBold" Margin="0,4,0,0" />
            <TextBlock x:Name="DocumentText" TextWrapping="Wrap" Margin="0,0,0,8" />

            <TextBlock Text="Password" Margin="0,8,0,0" />
            <PasswordBox x:Name="PasswordInput" Margin="0,0,0,10" />

            <CheckBox x:Name="UnlockDurationCheckBox" Margin="0,0,0,10" />

            <TextBlock x:Name="ErrorText"
                       Foreground="Firebrick"
                       Visibility="Collapsed"
                       TextWrapping="Wrap" />

            <Grid Margin="0,12,0,0" ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="1" Content="Cancel" Click="CancelButton_Click" />
                <Button Grid.Column="2" Content="Unlock" Click="UnlockButton_Click" />
            </Grid>
        </StackPanel>
    </Border>
</Window>
