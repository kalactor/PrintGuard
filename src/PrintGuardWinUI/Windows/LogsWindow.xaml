<Window
    x:Class="PrintGuardWinUI.Windows.LogsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewModels="using:PrintGuardWinUI.ViewModels"
    Title="PrintGuard Logs">
    <Border Padding="12">
        <ListView x:Name="LogsListView" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewModels:LogRowViewModel">
                    <Grid ColumnSpacing="10" Margin="0,2,0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="190" />
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind Timestamp}" />
                        <TextBlock Grid.Column="1" Text="{x:Bind Level}" />
                        <TextBlock Grid.Column="2" TextWrapping="WrapWholeWords" Text="{x:Bind Message}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Border>
</Window>
