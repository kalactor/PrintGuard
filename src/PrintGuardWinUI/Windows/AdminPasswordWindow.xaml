<Window
    x:Class="PrintGuardWinUI.Windows.AdminPasswordWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Admin authentication">
    <Border Padding="18">
        <StackPanel>
            <TextBlock Text="Enter admin password to continue."
                       FontSize="16"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"
                       Margin="0,0,0,10" />

            <TextBlock x:Name="ActionText"
                       Foreground="Gray"
                       TextWrapping="Wrap"
                       Margin="0,0,0,10" />

            <TextBlock Text="Password" Margin="0,6,0,0" />
            <PasswordBox x:Name="PasswordInput" Margin="0,0,0,10" />

            <Grid Margin="0,12,0,0" ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="1" Content="Cancel" Click="CancelButton_Click" />
                <Button Grid.Column="2" Content="Continue" Click="ContinueButton_Click" />
            </Grid>
        </StackPanel>
    </Border>
</Window>
